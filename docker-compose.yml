version: "3.8"

services:
  forex-bot:
    build: .
    container_name: forex-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config:/app/config
